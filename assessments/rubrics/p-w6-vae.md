# 评分量表 · p-w6-vae（W6 VAE 阶段关卡）

对应能力 Competencies: [SH9]
通过线 Pass: 每个维度 ≥ 3/4，且总分（四维之和）≥ 12/16（75%）

| 维度 | 4 优秀 | 3 合格 | 2 待改进 | 1 不足 |
|------|--------|--------|----------|--------|
| 正确性 Correctness | 全部 3 项自动测试通过（score=1.0）；`forward` 输出形状正确；训练 300 步 ELBO loss 明显下降；`encode` 输出潜维度正确；边界情况（不同 `n_in`/`n_latent`）均稳健 | 自动评分 ≥ 0.7（≥2 项通过）；主要流程正确但存在 1 处形状或 KL 计算遗漏 | 自动评分 0.5～0.7；VAE 结构至少有 1 部分完全实现（如编码器或解码器） | 自动评分 < 0.5；VAE 未实现或 `forward` 输出形状严重错误 |
| 代码质量 Code quality | 各函数单一职责，有中文 docstring；`reparameterize` 使用 `randn_like`（而非显式 `randn`）保证设备一致；无冗余代码；变量命名（`mu`、`logvar`、`x_hat`）清晰 | 基本可读，有少量冗余或风格问题（如重参数化中显式指定 device），但逻辑清晰 | 代码可运行但风格混乱；`encode`/`decode` 逻辑混写在 `forward` 中；docstring 缺失 | 代码难以阅读；KL 公式硬编码错误或使用错误的 PyTorch 模式（如 `detach` 阻断梯度） |
| 评估严谨性 Evaluation rigor | 训练损失曲线（loss vs epoch）图；≥1 张重构 vs 原始对比图（带轴标签和标题）；对潜空间可视化或重构误差有量化分析；对 β 超参数影响有讨论 | 有重构对比图但缺少损失曲线或轴标签；对结果的分析较浅 | 仅打印最终 loss 数值，无可视化；或图表无标注 | 无评估内容；或训练结果未展示 |
| 表达 Written/oral explanation | 笔记本 Markdown 清晰解释 ELBO = 重构 + KL 的数学含义；能口头说明重参数化技巧为何可以反向传播；能说明 β 的作用 | 大部分步骤有说明，但对 KL 或重参数化的解释不够深入；口头表达基本清楚 | Markdown 说明稀少；口头只能复述代码，不能解释 ELBO 或 KL 的含义 | 无文字说明；口头无法解释 VAE 与普通 AE 的区别 |

导师评语：

晋级判定（通过 / 补救复测）：