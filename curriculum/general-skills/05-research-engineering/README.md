# 研究工程（可复现性与指标）

## 练习 Exercises（本章 `assignments/` 下）
- ✍️ 编程 `01-repro`：在 `assignments/01-repro/starter.py` 里实现，然后 `python check.py 01-repro` 自测
- ✍️ 编程 `02-metrics`：在 `assignments/02-metrics/starter.py` 里实现，然后 `python check.py 02-metrics` 自测

## 小测 Quiz
- `assessments/quizzes/cap-research-eng.yaml`